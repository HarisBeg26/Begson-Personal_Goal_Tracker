{
  "name": "begson-goal-tracker",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "build": "npm --prefix client install && npm --prefix server install && npm --prefix client run build && npm --prefix server run build",
    "start": "NODE_ENV=production node server/dist/index.js",
    "dev": "npm --prefix client run dev & npm --prefix server run dev"
  }
}
